import Sheet from "@mui/joy/Sheet";

export default async function Dashboard() {
  return (
    <Sheet sx={{ p: 8, minHeight: "100vh" }}>
      <h1>Dashboard</h1>
      <p>Welcome to the dashboard!</p>
    </Sheet>
  );
}
